# Grafana Docker Dashboard

**Производственный дашборд Grafana для комплексного мониторинга Docker хостов и контейнеров**

[Русский] | [English](README.md)

Полное решение для мониторинга Docker окружений с использованием Prometheus, cAdvisor и node_exporter. Дашборд обеспечивает видимость метрик контейнеров и хостов в режиме реального времени с предварительно настроенными панелями для отслеживания использования ресурсов, производительности и состояния системы.

## Ключевые возможности

* **Единое представление** — Мониторинг всех Docker хостов и контейнеров с одного дашборда
* **Полный набор метрик** — CPU, память, дисковый I/O, сетевой трафик на уровне хостов и контейнеров
* **Отслеживание ресурсов** — Использование ресурсов в реальном времени с историческими трендами
* **Жизненный цикл контейнеров** — Отслеживание запущенных контейнеров, перезапусков и состояния здоровья
* **Поддержка нескольких хостов** — Мониторинг множества Docker хостов с фильтрацией через переменные
* **Системная информация** — Встроенная таблица версий Docker, ОС, ядра и экспортеров
* **Проверено в продакшене** — Боевой дашборд с нормализованными метриками и настроенными порогами
* **Готов к импорту** — JSON конфигурация готова для немедленного импорта в любой экземпляр Grafana
* **Гибкая фильтрация** — Динамические переменные для выбора job, node, port и временного интервала

## Архитектура

Дашборд использует стандартный стек мониторинга Prometheus:

```
┌─────────────────┐
│  Grafana        │ ← Визуализирует метрики
│  (Этот дашборд) │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  Prometheus     │ ← Собирает и хранит метрики
└────────┬────────┘
         │
         ├──────────────────┬─────────────────┐
         ▼                  ▼                 ▼
┌─────────────┐    ┌─────────────┐   ┌─────────────┐
│  cAdvisor   │    │ node_exporter│   │  cAdvisor   │
│  (Хост 1)   │    │  (Хост 1)   │   │  (Хост 2)   │
└─────────────┘    └─────────────┘   └─────────────┘
       │                   │                  │
       ▼                   ▼                  ▼
  Docker Engine      Системные метрики  Docker Engine
```

**Источники данных:**
* **cAdvisor** — Предоставляет метрики уровня контейнеров (CPU, память, сеть, файловая система)
* **node_exporter** — Предоставляет метрики уровня хоста (системные ресурсы, информация о железе)
* **Prometheus** — Агрегирует, хранит и предоставляет временные ряды данных
* **Grafana** — Визуализирует метрики через этот дашборд

![Предпросмотр дашборда](./images/dashboard-preview.png)

## Быстрый старт

### Требования

Вам понадобятся следующие компоненты:

1. **Prometheus** (версия 2.x или выше)
2. **cAdvisor** на каждом Docker хосте (обычно порт 8080)
3. **node_exporter** на каждом хосте (обычно порт 9100)
4. **Grafana** (версия 5.x или выше)

### Шаг 1: Развертывание экспортеров

На каждом Docker хосте запустите cAdvisor и node_exporter:

```bash
# Запуск cAdvisor
docker run -d \
  --name=cadvisor \
  --restart=always \
  --volume=/:/rootfs:ro \
  --volume=/var/run:/var/run:ro \
  --volume=/sys:/sys:ro \
  --volume=/var/lib/docker/:/var/lib/docker:ro \
  --volume=/dev/disk/:/dev/disk:ro \
  --publish=8080:8080 \
  --privileged \
  gcr.io/cadvisor/cadvisor:latest

# Запуск node_exporter
docker run -d \
  --name=node_exporter \
  --restart=always \
  --net="host" \
  --pid="host" \
  --volume=/:/host:ro,rslave \
  quay.io/prometheus/node-exporter:latest \
  --path.rootfs=/host
```

### Шаг 2: Настройка Prometheus

Добавьте конфигурацию сбора метрик в ваш `prometheus.yml`:

```yaml
scrape_configs:
  - job_name: 'cadvisor'
    static_configs:
      - targets:
          - 'docker-host-1:8080'
          - 'docker-host-2:8080'
    
  - job_name: 'node'
    static_configs:
      - targets:
          - 'docker-host-1:9100'
          - 'docker-host-2:9100'
```

Перезагрузите конфигурацию Prometheus:

```bash
curl -X POST http://prometheus:9090/-/reload
```

Проверьте, что цели активны: `http://prometheus:9090/targets`

### Шаг 3: Импорт дашборда в Grafana

**Вариант A: Импорт через UI Grafana**

1. Скачайте `10619_rev2.json` из этого репозитория
2. Откройте Grafana → **Dashboards** → **Import**
3. Загрузите JSON файл
4. Выберите источник данных Prometheus
5. Нажмите **Import**

**Вариант B: Импорт через API**

```bash
GRAFANA_URL="http://your-grafana:3000"
API_KEY="your-api-key"

curl -X POST "$GRAFANA_URL/api/dashboards/db" \
  -H "Authorization: Bearer $API_KEY" \
  -H "Content-Type: application/json" \
  -d @10619_rev2.json
```

**Вариант C: Автоматический провижининг**

Создайте `/etc/grafana/provisioning/dashboards/docker.yaml`:

```yaml
apiVersion: 1
providers:
  - name: docker
    orgId: 1
    folder: Infrastructure
    type: file
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
```

Скопируйте `10619_rev2.json` в `/var/lib/grafana/dashboards/` и перезапустите Grafana.

## Использование

### Переменные дашборда

Используйте выпадающие списки в верхней части дашборда для фильтрации данных:

* **job** — Выберите имя задачи cAdvisor (по умолчанию: `cadvisor`)
* **node** — Фильтр по конкретному хосту или выберите `All` для просмотра всех хостов
* **port** — Фильтр по порту или выберите `All` для агрегированного представления
* **interval** — Настройте разрешение временных рядов (auto, 1m, 5m и т.д.)

### Пояснение ключевых метрик

**Запущенные контейнеры**
* Показывает количество активных контейнеров за последние 5 минут
* Основано на метрике `container_last_seen` из cAdvisor

**Использование CPU**
* Использование CPU узла показывает агрегированную утилизацию CPU (0-100%)
* Использование CPU контейнерами нормализовано по доступным ядрам CPU
* Пороги: Предупреждение на 70%, Критический на 85%

**Использование памяти**
* Рабочая память (фактически используемая контейнерами)
* Кэш памяти (кэш файловой системы, освобождаемый)
* Доступная память хоста vs. общая

**Метрики диска**
* Свободное/занятое место по точкам монтирования
* Прочитано/записано байт по устройствам
* Операции I/O в секунду

**Сетевой трафик**
* Входящий/исходящий трафик на интерфейсах хоста
* Использование сети по контейнерам
* Накопительная пропускная способность

### Рекомендации

1. **Настройте оповещения** — Создайте алерты Prometheus для критических порогов (CPU > 85%, Память > 90%, Диск > 90%)
2. **Регулярный мониторинг** — Проверяйте таблицу версий для актуальности экспортеров и Docker
3. **Планирование мощностей** — Отслеживайте тренды за 7-30 дней для прогнозирования потребностей
4. **Оптимизация контейнеров** — Используйте метрики по контейнерам для выявления ресурсоемких нагрузок
5. **Настройка хранения** — Настройте период хранения Prometheus в соответствии с требованиями

## Диагностика

### Нет данных / значения "N/A"

**Проверьте цели Prometheus:**
```bash
curl http://prometheus:9090/api/v1/targets | jq '.data.activeTargets[] | select(.health != "up")'
```

**Убедитесь, что метрики существуют:**
```bash
# Проверка метрик cAdvisor
curl http://prometheus:9090/api/v1/query?query=container_last_seen

# Проверка метрик node_exporter
curl http://prometheus:9090/api/v1/query?query=node_memory_MemTotal_bytes
```

**Решение:** Убедитесь, что cAdvisor и node_exporter запущены и доступны из Prometheus.

### Панель "Запущенные контейнеры" показывает 0

**Проверьте переменные дашборда:**
* Установите `job = cadvisor` (или имя вашей задачи cAdvisor)
* Установите `node = All` и `port = All`

**Проверьте метрику напрямую в Prometheus:**
```promql
container_last_seen{job="cadvisor",image!=""}
```

**Решение:** Если нет результатов, проверьте, что cAdvisor корректно собирает данные о контейнерах. Проверьте логи cAdvisor.

### Панели памяти или диска пустые

**Проблема:** Несоответствие имен метрик с версией node_exporter

**Проверьте версию node_exporter:**
```bash
curl http://node-exporter:9100/metrics | grep node_exporter_build_info
```

**Решение:**
* Дашборд требует node_exporter >= 0.16 (использует суффикс `_bytes`)
* Старые версии используют другие имена метрик
* Обновите node_exporter или скорректируйте PromQL запросы в панелях

### Высокие значения CPU / проценты свыше 100%

**Причина:** Многоядерные системы без нормализации CPU

**Решение:** Дашборд уже включает нормализацию CPU. Если проблема сохраняется:
1. Убедитесь, что запрос использует `rate()` или `irate()` с подходящими интервалами
2. Проверьте, что CPU контейнеров нормализован по `machine_cpu_cores`
3. Смотрите CODE_QUALITY.md для деталей примененных исправлений

### Ошибки подключения источника данных Grafana

**Проверьте конфигурацию источника данных:**
1. Перейдите в Grafana → **Configuration** → **Data Sources**
2. Протестируйте подключение к источнику данных Prometheus
3. Убедитесь, что URL корректен (например, `http://prometheus:9090`)

**Проверьте сетевое подключение:**
```bash
# С контейнера/хоста Grafana
curl http://prometheus:9090/api/v1/status/config
```

**Решение:** Убедитесь, что Prometheus доступен из Grafana. Проверьте правила файрвола и DNS.

### Переменные не загружаются

**Симптом:** Выпадающие списки переменных пустые или показывают "No data"

**Проверьте значения меток Prometheus:**
```promql
# Проверка меток job
label_values(container_last_seen, job)

# Проверка меток instance
label_values(container_last_seen, instance)
```

**Решение:**
* Убедитесь, что метрики имеют ожидаемые метки
* Проверьте regex паттерны переменных в настройках дашборда
* Убедитесь, что источник данных Prometheus выбран корректно

## Профессиональные SRE/DevOps услуги

Этот дашборд поддерживается **run-as-daemon** — профессиональным провайдером SRE/DevOps услуг, специализирующимся на производственном мониторинге, высокой доступности и автоматизации инфраструктуры.

Нужна помощь помимо этого дашборда? Мы предлагаем:

* **Настройка production мониторинга** — Полное развертывание стека observability (Prometheus, Grafana, Alertmanager, Loki)
* **Кастомные дашборды и алерты** — Индивидуальные решения для мониторинга вашей инфраструктуры
* **Конфигурация высокой доступности** — HA Prometheus, кластеризация Grafana, disaster recovery
* **Тюнинг производительности** — Оптимизация Docker, Kubernetes и баз данных под высокие нагрузки
* **Infrastructure as Code** — Автоматизация инфраструктуры мониторинга через Terraform, Ansible
* **Дежурная поддержка** — 24/7 реагирование на инциденты и устранение неполадок

**Связаться с нами:** [https://run-as-daemon.ru](https://run-as-daemon.ru)

Нужна ли вам разовая консультация или постоянное управление инфраструктурой — мы предоставляем SRE услуги экспертного уровня для надежности и производительности ваших систем.

## Поддержка / Спонсорство

Если этот дашборд экономит ваше время или помогает в мониторинге инфраструктуры:

### Бесплатная поддержка
* ⭐ **Поставьте звезду репозиторию** — Помогите другим найти этот дашборд
* 🔗 **Поделитесь** — Расскажите в соцсетях, блогах или коллегам
* 💬 **Внесите вклад** — Сообщайте о проблемах, предлагайте функции, отправляйте улучшения

### Финансовая поддержка
* 💰 **GitHub Sponsors** — Используйте кнопку "Sponsor" в верхней части репозитория
* ☕ **Угостите кофе** — Поддержите текущее обслуживание и обновления
* 🎯 **Закажите проект** — Закажите кастомные дашборды или решения для мониторинга

Ваша поддержка помогает поддерживать этот проект и создавать больше open-source инструментов мониторинга для сообщества.

**Варианты спонсорства:** См. [.github/FUNDING.yml](.github/FUNDING.yml) для всех доступных каналов.

## Участие в разработке

Мы приветствуем вклад! См. [CONTRIBUTING.md](CONTRIBUTING.md) для руководства по:
* Сообщению об ошибках и запросу функций
* Отправке pull request'ов
* Ожиданиям по стилю кода и тестированию

## Лицензия

Этот проект распространяется по лицензии **Unlicense** (эквивалент общественного достояния).

Вы можете свободно использовать, изменять и распространять этот дашборд без каких-либо ограничений. См. файл [LICENSE](LICENSE) для деталей.

---

**Поддерживается [run-as-daemon](https://run-as-daemon.ru)** | **Вопросы?** См. [SUPPORT.md](SUPPORT.md)
